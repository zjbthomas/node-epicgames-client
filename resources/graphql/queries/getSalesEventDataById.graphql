query getSalesEventDataById($salesEventId: String!) {
    PriceEngine {
        salesEvent(salesEventId: $salesEventId) {
            endDate
            eventSlug
            grantInitialVoucher
            id
            voucherImages {
                type
                url
            }
        }
    }
}
